{
  "snowflake_connection": {
    "account": "YOUR_ACCOUNT.snowflakecomputing.com",
    "warehouse": "YOUR_WAREHOUSE",
    "database": "YOUR_DATABASE",
    "role": "YOUR_ROLE",
    "auth_type": "OIDC",
    "oidc_client_id": "YOUR_OKTA_CLIENT_ID",
    "oidc_issuer_url": "https://YOUR_OKTA_DOMAIN.okta.com/oauth2/YOUR_AUTH_SERVER_ID",
    "oidc_redirect_uri": "http://localhost:8080/callback",
    "oidc_scope": "openid profile email",
    "query_timeout": 300,
    "keep_alive": true,
    "use_high_precision": true
  },
  "example_usage": {
    "description": "Example of how to use OIDC authentication with Snowflake extension",
    "steps": [
      "1. Configure your Okta application with the redirect URI",
      "2. Set the OIDC configuration parameters above",
      "3. Use the extension with OIDC auth type",
      "4. The extension will guide you through the OIDC flow"
    ],
    "connection_string_example": "account=YOUR_ACCOUNT;warehouse=YOUR_WAREHOUSE;database=YOUR_DATABASE;role=YOUR_ROLE;auth_type=OIDC;oidc_client_id=YOUR_CLIENT_ID;oidc_issuer_url=https://YOUR_OKTA_DOMAIN.okta.com/oauth2/YOUR_AUTH_SERVER_ID;oidc_redirect_uri=http://localhost:8080/callback"
  },
  "security_notes": [
    "PKCE (Proof Key for Code Exchange) is used for security",
    "State parameter prevents CSRF attacks",
    "Authorization code is exchanged for tokens securely",
    "Tokens are used for Snowflake authentication"
  ]
}
